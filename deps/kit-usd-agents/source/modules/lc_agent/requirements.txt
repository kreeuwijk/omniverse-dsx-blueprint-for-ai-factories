# This file lists all Python dependencies for the LC Ai Agent project.
# Some dependencies, such as usd-core, may not be available via PyPI and
# need to be installed manually or be present in the environment.

# We're using langchain >= 1.2.7 (CVE-2025-68664 fix) across all components
langchain-core>=1.2.7,<2.0.0
langchain_classic
langsmith>=0.6.3

aioredis==2.0.1
langchain>=1.2.7,<2.0.0
langchainhub
libcst==1.5.0
tiktoken
toml==0.10.2