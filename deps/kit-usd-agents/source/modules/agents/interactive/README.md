lc_agent_interactive
====================

Base for building interactive assistants around arbitrary Python helper modules.
It injects module API (first-level public functions + docstrings) and multiple
RAG retrievers (by name) into the system prompt.


